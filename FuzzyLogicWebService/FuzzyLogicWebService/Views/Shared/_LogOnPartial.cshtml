@if(Request.IsAuthenticated) {
    <text>@Resources.Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink(@Resources.LogOff, "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink(@Resources.LogOn, "LogOn", "Account") ]
}
